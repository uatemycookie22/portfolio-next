'use client';
import EmojiEventsIcon from '@mui/icons-material/EmojiEvents';

export default function MEmojiEventsIcon (props: Parameters<typeof EmojiEventsIcon>[0]) {
	return (<EmojiEventsIcon {...props} />)
}